package net.rezmason.scourge.model.aspects;

import net.rezmason.scourge.model.ModelTypes;

class BiteAspect extends Aspect {
    public static var NUM_BITES:AspectProperty = {id:Aspect.ids++, initialValue:0};
}
